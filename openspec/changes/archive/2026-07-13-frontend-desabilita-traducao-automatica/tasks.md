## 1. Implementação

- [x] 1.1 `frontend/index.html`: adicionar `<meta name="google" content="notranslate">` no `<head>`
- [x] 1.2 `frontend/index.html`: corrigir `<html lang="en">` para `<html lang="pt-BR">`

## 2. Validação

- [x] 2.1 Build (`npm run build`) sem erros
- [x] 2.2 Inspecionar o HTML servido (view-source ou DevTools) e confirmar a meta tag e o `lang` corretos
- [ ] 2.3 No Chrome, confirmar que o ícone/prompt de tradução automática não aparece mais para a aplicação (testar numa aba nova, sem tradução manual já acionada)
- [x] 2.4 Confirmar que nenhuma outra funcionalidade foi afetada (navegação, filtros, ordenação) — mudança é só no HTML base
